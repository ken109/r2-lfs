{
  "name": "r2-lfs",
  "version": "0.1.0",
  "private": true,
  "description": "A Git LFS server on Cloudflare Workers that stores objects in R2",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit && tsc --noEmit -p scripts",
    "test": "vitest run",
    "gc": "node scripts/gc.ts"
  },
  "cloudflare": {
    "bindings": {
      "BUCKET": {
        "description": "R2 bucket that holds the LFS objects."
      },
      "ALLOWED_OWNERS": {
        "description": "Comma-separated GitHub users or orgs that may use this server, e.g. `my-name,my-org`. Required. Use `*` only if you really mean anyone."
      },
      "AUTH_MODE": {
        "description": "`github`: authenticate with a GitHub token and mirror the repository's permissions. `token`: use the static tokens in `AUTH_TOKENS`."
      },
      "STORAGE_LAYOUT": {
        "description": "`per-repo`: store objects under `<owner>/<repo>/` so each repository can be cleaned up on its own. `shared`: one pool deduplicated across repositories."
      },
      "TRANSFER_MODE": {
        "description": "`auto`: use presigned URLs when the R2 API credentials below are set, otherwise proxy through the Worker. `proxy` caps uploads at `PROXY_MAX_UPLOAD_MB`."
      },
      "PROXY_MAX_UPLOAD_MB": {
        "description": "Largest upload accepted in proxy mode. Match your plan's request body limit (Free/Pro: 100)."
      },
      "R2_ACCOUNT_ID": {
        "description": "Only for presigned URLs. Your Cloudflare account ID (Dashboard > Workers & Pages, right sidebar)."
      },
      "R2_BUCKET_NAME": {
        "description": "Only for presigned URLs. Must be the same name as the bucket bound to `BUCKET`."
      },
      "R2_ACCESS_KEY_ID": {
        "description": "Only for presigned URLs. Create an R2 API token with Object Read & Write on the bucket (R2 > Manage API tokens)."
      },
      "R2_SECRET_ACCESS_KEY": {
        "description": "Only for presigned URLs. The secret that pairs with `R2_ACCESS_KEY_ID`."
      },
      "AUTH_TOKENS": {
        "description": "Only for `AUTH_MODE=token`. Comma-separated `<scope>:<r|rw>:<token>` entries, e.g. `my-name/*:rw:<random>`. Leave empty in github mode."
      }
    }
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260911.1",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "~4.1.0",
    "wrangler": "^4.131.1"
  }
}
