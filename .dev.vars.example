# Presigned URLs only. Leave empty to proxy transfers through the Worker.
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
# AUTH_MODE=token only. Comma- or newline-separated <owner/repo|owner/*|*>:<r|rw>:<token of 16+ chars>
AUTH_TOKENS=
